[2026-05-28] — Added polaroid photo gallery to week-02: 6 cards fly in on scroll, float continuously, straighten on hover. Placeholders ready for real images.
[2026-05-28] — Week-01 scroll flight: plane approaches from off-screen left, banks level, and lands at center as user scrolls through the hero section.
[2026-05-28] — Week-01: removed highlights section (low-value placeholder cards).
[2026-05-28] — Removed highlights section from all 10 week pages.
[2026-05-28] — Week-02: replaced fixed float-canvas with inline interactive canvas below "Aprenent i disfrutant" — 3 draggable wireframe shapes (icosahedron, torus knot, octahedron), drag to rotate, auto-spin when idle.
[2026-05-28] — Week-01: replaced SVG path plane with GLB clone — small Boeing 737 follows a CatmullRomCurve3 S-path through the 3D scene, scroll-driven via ScrollTrigger scrub, auto-banks along tangent.
[2026-05-28] — Refactored scripts: three-week.ts = base (wireframe + shared GSAP); three-week-01.ts = GLB/path-plane; three-week-02.ts = migrated from models-week02.ts; three-week-03 to 10 = stubs. Each week imports its own script.
[2026-05-28] — Created immersive gallery page (/gallery): Three.js scroll-driven camera flies through 10 glowing week frames; click opens CSS3D carousel; click photo opens lightbox. Data in src/data/gallery.ts with English subtitles and placeholder URLs.
[2026-05-28] — Wired gallery to Supabase Storage (bucket: Photos, folders: week1…week10). Client-side fetch via REST API (no SDK). Env vars SUPABASE_URL + SUPABASE_KEY injected server-side at build, passed to client via JSON script tag.
[2026-05-28] — Rebuilt gallery as clean site-style page: same nav/fonts/card system as week pages. Shimmer skeletons while photos load, lightbox on click, 10 week sections with Supabase-fetched photos.
[2026-05-29] — gallery.astro: fixed garbled footer text (Ã— → ×) and lightbox close button (œ• → ✕); added console.logs to debug Supabase photo loading (config check, per-folder fetch URL, HTTP status, file list, filtered count).
[2026-05-29] — gallery.astro: replaced raw fetch Supabase calls with @supabase/supabase-js SDK (createClient, storage.from().list(), storage.from().getPublicUrl()).
[2026-05-29] — Supabase Storage fixed: added RLS SELECT policy for anon role on bucket Photos + enabled public bucket. Photos now load in gallery. Removed debug console.logs.
[2026-05-29] — Gallery: show first 6 photos, gradient fade overlay on grid bottom, minimal "↓ N more" text trigger reveals rest.
[2026-05-29] — three-gallery.ts: new script for gallery page with photo_cam.glb (Draco), material transparency fix, green particles. three-index.ts restored to globe for main page.
[2026-05-29] — Week-01 and Week-02: corrected English grammar and phrasing (quantifiers, tense consistency, word order, subject omissions).
[2026-05-29] — Added EU flag favicon (/photos/Flag_of_Europe.svg.webp) to all pages (index, WeekLayout, gallery).
[2026-05-29] — Translated entire site to English: html lang attributes, meta descriptions, nav/hero/footer labels, all page content (weeks 01–10), weeks.ts subtitles and dates.
[2026-05-30] — Week-02: full-screen water ripple shader background (WebGL, mouse + scroll spawn ripples); glassContent mode enabled so frosted sections let water show through.
[2026-05-30] — Week-02: water shader alpha lowered to 0.55; hero lifted to z-index:3 so text/models stay above water; removed mouse-move ripple trigger (scroll only).
[2026-05-30] — Week-02: water-canvas JS-moved to body before #three-canvas so z-index:-1 is root-context-relative (was trapped inside .week-content z-index:10 stacking context).
[2026-05-30] — Week-02: temperature "40ºC!" blinks red forever via CSS step animation.
[2026-05-30] — Week-02: added GLB loader to three-week-02.ts — Sun.glb loads on #three-canvas with golden lighting, slow rotation, particle field, mouse parallax.
[2026-05-30] — Gallery: camera model lowered by 1.5 units (position.y offset in tick loop).
[2026-05-30] — Added utils/convert-upload.mjs: converts raw photos to webp@75%, renames to weekXX-NN.webp, uploads to Supabase bucket Photos/weekXX/. Fixed .gitignore (was ignoring entire utils/, now only utils/output/).
[2026-05-30] — Gallery: "↓ N more" expand now togglable — collapse button (↑ Show less) removes extra photos, restores fade + show-more, scrolls section into view.
[2026-05-30] — Renamed week-10 → week-11 (11-week Erasmus); created new week-10 placeholder; fixed all data refs in weeks.ts, gallery.ts, routes.ts; fixed broken three-week-10.ts import in week-11.astro.
[2026-05-31] — Added secret party page (/party): password-gated (FIESTA), dark neon theme, Three.js disco sphere + colored confetti particle clouds + orbiting lights, polaroid photo grid from Supabase Photos/party/ folder.
[2026-05-31] — Party page: switched polaroid grid from fixed-column grid to CSS masonry (columns), removed forced 4:3 aspect ratio — photos now display at natural proportions.
[2026-05-31] — Party page: fixed photo size (grid minmax 150-190px capped), restored 4:3 cover crop for uniform polaroids; replaced static lightbox with prev/next carousel (arrow keys + click buttons, fade transition, photo counter).
[2026-05-31] — Party page: rewrote carousel with GSAP — overlay fade+scale open/close, slide-left/right between photos with busy guard; fixed position:fixed-inside-fixed bug by switching nav buttons to position:absolute.
[2026-05-31] — Party page: full GSAP polaroid system — staggered chaos-drop entrance per card, perpetual independent float (y+rotation), hover lifts+straightens+neon-glow via paused float tween, resume float on mouseleave.
[2026-05-31] — Party page: fixed oversized photos — switched polaroid grid from CSS grid (minmax not constraining) to flex-wrap with hard width:175px per item; shrank Three.js particles (0.07→0.032) and ring opacity (0.35→0.18).
[2026-05-31] — Party page: reverted confetti/orbs GSAP experiment — restored Three.js 3D background (three-party.ts); kept GSAP carousel + polaroid animations.
[2026-05-31] — Party page: fixed carousel (moved outside #party-content — was trapped in its stacking context/opacity:0); fixed photo size (grid repeat(auto-fill, 160px) strict fixed columns, no stretch).
[2026-05-31] — Party page: replaced polaroid+carousel entirely with gallery.astro pattern — photo-grid, photo-item, simple lightbox (click to zoom, Escape/click-outside to close), show-more button. Same behaviour as weekly gallery pages.
[2026-06-01] — Party page: fixed gate title "PARTY" appearing shifted right — added text-indent:-0.04em to compensate letter-spacing optical centering offset.
[2026-06-01] — Easter eggs: (1) type "party" on any page → redirect to /party; (2) the × between Jose and Álvaro in nav is a hidden link to /party (invisible, cursor:default, works on mobile tap).
[2026-06-02] — Party gate: fixed PARTY title centering — translateX(-8%) scoped to min-width:769px so desktop centers correctly without breaking mobile.
[2026-06-03] — Week-03: full festival theme (Festa de São Gonçalo) — dark night bg (#0e0014), scroll-triggered 3D fireworks on fixed canvas (rockets rise + burst in Three.js perspective with depth/flash sphere), inline interactive 3D fireworks launcher (click/tap to shoot), star field, GSAP SVG draw "São Gonçalo", stat cards, nav/weekstrip/hero/footer dark-mode overrides via is:global.
[2026-06-03] — Week-03: restored rocket Line trails (20-point shift-history), restored central octahedron wireframe + orbital particle cloud, fixed burst double-creation bug (Points added to scene once), faster burst velocity + bigger flash for instant pop feel.
[2026-06-03] — Week-03: fixed invisible trails in click-to-shoot canvas — swapped THREE.Line → THREE.Points (linewidth not supported in WebGL so lines were 1px invisible), raised rocket start from y=-8 to y=-6.5 so trail enters inline canvas viewport from frame 1.
[2026-06-03] — Week-03: removed click-to-shoot inline canvas section (not adding value).
[2026-06-03] — Week-03: added 42 spinning 3D confetti ribbons (BoxGeometry, festival colors, per-piece random rotation/drift, sine-wave sway, loop from bottom when exiting top).
[2026-06-08] — Week-03: replaced octahedron wireframe with Star.glb model (scale 3.8) as central 3D object; loaded via GLTFLoader with pink wireframe material matching week color.
[2026-06-08] — Week-03: Star model now full solid mesh (MeshStandardMaterial, pink+emissive+metalness), Y-axis-only spin, three-point lighting (ambient + pink directional + orange fill).
[2026-06-08] — Week-02: fixed double border on photos — each photo-item had a redundant nested photo-item div, removed inner wrapper.
[2026-06-08] — Gallery: synced all week subtitles in gallery.ts to match weeks.ts (weeks 1, 3, 4, 6, 7, 8, 9, 10, 11 were stale or mismatched).
[2026-06-08] — Party page: fixed week-dot styles not applying — moved .week-dot/.week-strip overrides from scoped <style> to <style is:global> so they reach inside WeekStrip child component. Now matches week-03 navbar exactly.
[2026-06-08] — Party gate: escape link now a pill button with breathing pulse animation (opacity 0.45↔1 loop), pauses on hover with full white; text "← take me back home". Escape key still works too.
[2026-06-08] — Week-04: full WIP construction page — 3D gold cubes fall+stack with bounce (GSAP), rotating scaffold wireframe, construction tape banners, pulsing warning lights, fake terminal typewriter with build logs, progress bar that never finishes.
[2026-06-08] — Week-03: festival string lights (14 colored glowing bulbs strung across top of section, async flicker), neon flicker heading on "NIGTHS IN AMARANTE", sparkle cursor trail (pink/orange/gold dots float up from mouse while over content).

[2026-06-08] — Analytics: added @vercel/analytics to all pages (WeekLayout, index, gallery, party) via default import from @vercel/analytics/astro.

[2026-06-09] — Week-04: full zen redesign — dark forest night sky (bgColor #0b1a12), layered mountain silhouette scene (6 PlaneGeometry ranges with displaced vertices + fog), moon with pulsing PointLight, drifting mist particles, stars; content replaced with frosted-dark-glass sections: kanji intro (友), serif haiku, Ma emptiness placeholder.
[2026-06-09] — Week-04: zen scene updated to original warm gold/orange palette (#FFD700/#FF6B35/#fffbf0) — golden-hour mountains with sun sphere, warm-earth silhouettes, golden dust particles; added photos section (6 Supabase week4 images); removed dark overrides.
[2026-06-09] — Week-04: added zen bird — 5-vertex line-art silhouette flies across sky randomly (12-18s crossing, 7-19s pause between), asymmetric wing flap (harder up, softer down), CatmullRom arc path, faces direction of travel.
[2026-06-09] — Week-04: replaced line-art bird with Bird.glb (GLTFLoader + AnimationMixer for wing flap), bird spawns at z=9-11 (in front of all mountains), faces direction of travel via curve tangent.
[2026-06-11] — Week-04: bird scale reduced 0.25→0.015; replaced time-based random flight with scroll-driven path (ScrollTrigger); two barrel rolls (yaw*pitch*roll quaternion composition) smoothstep-eased at ~25-48% and ~60-83% scroll; S-curve flight path with altitude swoops.
[2026-06-11] — Week-04: fixed bird y-position — was flying at y=6.8–9.2 (above camera frustum, off-screen); moved to y=3.8–5.2 to land within visible band at z≈10.
[2026-06-11] — Week-04: fixed bird orientation — model exported with body along +Y (spinning like helicopter); wrapped in pivot group, baked rotation.x=-π/2 on raw model so body aligns with +Z forward axis; barrel rolls now spin correctly around nose-to-tail axis.
[2026-06-11] — Week-04: fixed bird flying backwards (showing tail to camera) — changed rotation.x correction from -π/2 to +π/2 so nose maps to +Z and yaw drives it in correct flight direction.
[2026-06-11] — Week-04: fixed bird orientation definitively — raw model has forward=+X, up=+Z; applied quaternion(-0.5,-0.5,-0.5,0.5) (120° around (1,1,1)/√3) to remap forward→+Z and up→+Y in pivot-local space.
[2026-06-11] — Week-04: removed barrel rolls; bird now glides straight top-left to bottom-right (y 5.2→2.0, x -28→28) across scroll.
[2026-06-11] — Week-04: content revision — battery charging animation (CSS fill + JS counter, orange→gold colour shift), Tâmega river section (shorter/punchier copy, darker readable text), 3 tilted vibe cards (💤🏊🌅, rotate-on-hover CSS), removed daily log and numbers sections.
[2026-06-11] — Week-04: readability pass — removed 友 kanji + gold glow animations; all font-weight:300 headings → 500; all low-opacity faint text boosted to 0.55–0.82; active bat-line orange darkened to #c85000.
[2026-06-11] — Week-04: removed river wave SVGs, marquee strip, and "The river" section-tag; centered battery section content in frame.
[2026-06-11] — Index: added "journey home" countdown — live days/hours/min/sec to departure (Jul 31 22:00 +01:00), progress bar with ✈ marker showing % of adventure lived, weeks-lived/weeks-left/days-to-go stats. Self-contained section + inline script.
[2026-06-11] — Index countdown: START date corrected to 2026-05-20; added frosted-glass backdrop panel (blur+saturate, semi-opaque bg, green border) behind countdown for readability over globe canvas — globe left untouched to preserve hero composition.
[2026-06-11] — Index countdown: added finished state at departure — clock hidden, "✈ Home. / 11 weeks, lived." message, copy flips to past tense, bar locks 100%, panel gets celebratory green glow pulse, interval cleared. Tested via temp past DEPART then reverted to 2026-07-31.
[2026-06-11] — Index countdown bugfix: .cd-done/.cd-clock display:flex was overriding the [hidden] attribute (class beats [hidden] specificity), so "Home." finished-state showed permanently. Added .cd-clock[hidden],.cd-done[hidden]{display:none}.

[2026-06-12] — Made progress-bar plane (index) bigger (1.1rem→2rem) so green fill reads as a trail behind it.
[2026-06-12] — Week-04 section titles (zen/battery/tamega) now match other weeks: weight 900, UPPERCASE, solid color, removed gradient.
[2026-06-12] — Week-04 titles now exact-match global h2 size/line-height: clamp(2rem,5vw,4rem), line-height 0.95.
[2026-06-12] — Week-04 prose paragraphs (.tamega-body p) now match global .content-section p: 1.1rem, line-height 1.75, opacity 0.65.
[2026-06-12] — Week-04 vibes: added 4th card "Until late" (talking by river past midnight); grid 3→4 cols, 2-col at <=880px.
[2026-06-12] — Week-04 vibes grid back to 3 cols (user kept 3 cards).
[2026-06-12] — Week-04: replaced haiku/Iku section with 3D incense scene (incense-week04.ts): vertical stick + flickering ember + shader-based smoke particles rising/swaying/fading. New #incense-canvas; removed haiku CSS.
[2026-06-12] — Removed leftover incense import (was throwing on missing #incense-canvas → broke battery script) + deleted incense-week04.ts and incense CSS.[2026-06-17] — Week-05 theme set to "Better together" (friendship). New connection-network bg scene (three-week-05.ts): cyan/green nodes drift + draw bonds when near + cursor pull + parallax. Page: word-by-word headline reveal, friendship story, bond cards (kayak/basketball/crew), tilt photos. modelPath=skip, glass content. weeks.ts subtitle updated.
[2026-06-17] — Week-05: removed cursor node-pull (parallax kept); photos no longer tilted (straight, hover scale only).
[2026-06-17] — Week-05: replaced dot nodes with Star.glb clones (30, normalized + tinted cyan/green, gentle spin) = friendship constellation; bonds/lines + parallax kept. Added lights, GLTFLoader.
[2026-06-17] — Week-05: "A week of good company" headline now GSAP scatter→gather (words blur in from random dirs/rotation, converge on scroll); split into .story-word spans, "good company" accent cyan.
[2026-06-17] — Week-05: story prose color now matches other pages (dropped teal rgba(5,58,68,0.72) → opacity 0.65 over default text, like global .content-section p).
[2026-06-22] — Index: fixed mobile horizontal scroll. .site-title clamp min 5rem→3.2rem so "AMARANTE" fits narrow phones (desktop 15vw unchanged); added overflow-x:hidden to html as root-scroller guard.
[2026-06-22] — Week-06: full "São João in Porto" night theme (warm palette primary #FFB02E / accent #FF5470 / bg #0a0612). Published week 6 (routes.ts); updated weeks.ts + gallery.ts subtitle/colors.
[2026-06-22] — Week-06 redesign (own identity, dropped week-03 borrowals): removed fireworks, string lights, neon-flicker heading, sparkle cursor trail. three-week-06.ts now: rising sky lanterns (balões, body+flame core+halo sprite, sway/flicker/respawn) as sole motion; rebuilt Dom Luís I bridge properly — solid ExtrudeGeometry parabolic iron arch + straight upper/lower deck beams + end towers + full lattice truss (posts + X-diagonals) between decks + spandrel posts + warm additive deck lights, lit by warm key + pink rim so edges catch gold; faint mirrored reflection on Douro shimmer band; star field; mouse parallax. Page: warm gradient .glow-title (own signature, no flicker), kept quadra + 4 tradition cards + bridges section + photos.

[2026-06-22] — Week-06: replaced procedural Dom Luís I bridge in three-week-06.ts with /models/bridge1.glb (GLTFLoader). Model auto-normalized to span width, recentered at (0,-2.5,Z=-3); mirrored water reflection rebuilt by cloning loaded model. Lanterns/river/stars/lighting/parallax kept. Removed now-unused procedural arch/deck/tower/lattice code.
[2026-06-23] — Week-06: bridge.glb now blue steel (MeshStandardMaterial color 0x4f7aa6, metalness 0.95, roughness 0.38, override on all model meshes) and bigger — HALF span 15→24 to fill the screen width.
[2026-06-23] — Week-06: fixed bridge parts vanishing into dark bg after blue-steel change — metalness 0.95→0.65, roughness 0.38→0.5, added emissive 0x14283f (intensity 0.6) so faces away from lights stay visible.
[2026-06-23] — Week-06: fixed top road + outer arch missing on bridge — overriding GLB material with default FrontSide steelMat culled thin surfaces whose normals face away; added side: THREE.DoubleSide to restore them.
[2026-06-25] — Week-06: added GSAP ScrollTrigger reveals in three-week-06.ts — section tags/headings/paragraphs fade up, trad-cards pop in keeping their --rot tilt, quadra slides in from left, photos rise+scale in stagger.
[2026-06-25] — Week-06: added river surface under the bridge — makeWaterTexture() vertical gradient (warm waterline → deep night) on a 44x14 plane at y=WATER_Y-7, z=-6, behind the reflection. Fills the bottom void so the shimmer points + mirrored bridge read as actual water.
[2026-06-26] — Week-06: reworked the water under the bridge — widened gradient plane (70x16) + shimmer points to ±60 full width so water no longer sits in a strange center-only band; added makeStreakTexture() warm vertical reflection streaks (30 swaying/flickering sprites) across the river for São João lantern-on-water feel.
[2026-06-26] — Week-06: fixed lantern pop/teleport — each lantern now fades in (y −11→−7) and out (y 13→17.5) via smoothstep on body/core/halo opacity, so the recycle jump at y>18 happens while fully invisible instead of snapping on-screen.
[2026-06-26] — Week-06: added scroll-triggered martelinho (Hammer.glb) — ScrollTrigger on .traditions-section plays a GSAP timeline that swings the hammer in from off-screen top-right, strikes at IMPACT point, fires an 80-particle additive spark burst (radial velocity + gravity + fade) and a decaying camera shake, then elastic-recoils and exits off-screen. Replays on onEnter/onEnterBack.
[2026-06-26] — Week-06: reworked martelinho to be scroll-scrubbed (scrub:1, start top 85% → end top 35%) instead of one-shot onEnter, so the swing/strike is driven directly by scroll position; sparks fire via ScrollTrigger onUpdate when the playhead crosses the 0.45 impact frame. Moved IMPACT to lower-left gutter (-5,-2.2,6) so the hammer lands in clear canvas instead of hiding behind the centered glass card column.
[2026-06-26] — Week-06: hammer now renders on a dedicated full-screen overlay canvas (z-index 50, pointer-events none) with its own scene+lights, drawn each frame with the shared camera — so the martelinho + sparks sit in front of the glass cards instead of behind them. Moved IMPACT to centre/foreground (0,0.5,8). Lengthened + softened the scrub trigger (start top bottom → end bottom top, scrub 1.5, power2.in) so the swing is gentle over the whole section instead of brute/abrupt.
[2026-06-26] — Week-06: halved hammer size (scale 7→3.5) and re-anchored its TOP (box.max.y) to the pivot origin so the head is what lands on IMPACT — sparks/shake now coincide with the head actually striking instead of the model centre missing the point.
[2026-06-26] — Week-06: stood the martelinho upright (base rotation.z = π/2 on the model) so the mallet head's face points up, then anchored that top face (box.max.y after rotation) to the pivot origin — combined with the swing's rotation it now tilts and strikes IMPACT with the head face instead of lying flat on its belly.
[2026-06-26] — Week-06: redid martelinho motion — hammer now stands upright on its handle (base anchored at box.min.y to the pivot) and tips over to the right as you scroll (rotation.z 0 → -1.5, power2.in accelerating fall + tiny settle). Sparks + shake fire once when the tilt crosses NEAR_FLAT (-1.27 rad ≈ 17° off the ground), spawned at a head marker's world position so they land on the head. Hammer hidden until the trigger is active (onToggle).
[2026-06-26] — Week-06: martelinho now reads as a real swing — hand-pivot just inside the right edge, hammer starts cocked off-screen right (START_ROT -0.8), arcs overhead and tilts to the LEFT (rotation → 1.4, power2.in accelerating downswing) and the head slams down. Spark + shake fire as the swing crosses NEAR_FLAT (1.27 rad ≈ 17° off the ground), spawned at the head marker. Tiny settle after the hit.

[2026-06-26] — Week-06: smoother martelinho hand-swing. Hammer now drops in from off-screen ABOVE held vertical (cocked, START_ROT -0.1), descends centred to y -1.5, then tilts LEFT in an arc (rotation 0.05 → 1.15, power2.in) like an arm swinging. Spark + shake fire as the tilt crosses SPARK_ROT (1.0), then it swings on through (z → 1.9) and lifts back off-screen (y 10, x -5), disappearing.
[2026-06-26] — Week-06: fixed martelinho orientation. Model is already upright natively (handle along -Y, head cylinder along X on top, striking face = curved underside), so removed the bogus h.rotation.z = π/2 that was laying it flat / hitting with the wrong part. Spark marker moved to head centre (0.76 of height). Entrance now flies in from the top-RIGHT corner (start 8,9) to centre (0,-1.5) held vertical, instead of dropping straight down.
[2026-06-26] — Week-06: martelinho timing tweaks. Trigger fires earlier (start top bottom+=40%, end center center) so the swing happens sooner in the scroll. Tilt now overlaps the fly-in (rotation START_ROT → SPARK+0.15 over 0.05–0.6) so the hammer is already swinging left as it flies in from the top-right, not after it centres.
[2026-06-26] — Week-06: synced martelinho hit. Decoupled the spark trigger from the tilt anchor — added SPARK_AT (0.8) so sparks + shake now fire earlier in the downswing instead of at SPARK_ROT (1.0) near the peak. Tilt motion unchanged.
[2026-06-26] — Week-06: fixed martelinho stranding on fast scroll. Changed scrub 1.5 → true so the swing tracks scroll position exactly instead of lagging 1.5s behind. The lag was leaving the hammer frozen mid-screen (or hidden before reaching its off-screen exit) when the trigger toggled at the range edges; with scrub:true the position always matches progress (off-screen at both ends).
[2026-06-26] — Week-06: smooth martelinho without stranding. Brought back scrub smoothing (1.2) but removed the binary onToggle visibility flip — that was the real cause of the mid-screen freeze (it hid/showed the hammer out of sync with the lagged tween). Now the tween always runs and eases to its clamped ends (both off-screen), so fast scroll glides it off-screen instead of snapping. Hammer set visible once the GLB loads.
[2026-06-26] — Week-06: removed the "rules of the night" (traditions) section — low info value. Repointed the martelinho ScrollTrigger from .traditions-section to .bridges-section (next section, similar scroll slot) so the hammer swing still plays. Deleted the now-orphaned trad-card CSS and the trad-card reveal GSAP block.[2026-06-26] — Index: week-06 card text unreadable (black on dark #0a0612 bg). Added isDark() luminance check; cards with dark bg now render card-num/card-dates in white. card-week-title already used primaryColor.
[2026-06-26] — Index: dark week cards (week-06) had invisible hover (black shadow on dark bg). Refactored isDark cards to .week-card--dark class: white text + hover lightens border (rgba 255,255,255,.35) and deepens shadow so the lift reads like the light cards.
[2026-06-26] — Index countdown: weeks-left was 11 - floor(elapsed/7), assuming a 77-day trip, but real span (May 20 → Jul 31 22:00) is ~72.9 days (~10.4 weeks), so it overstated (showed 6). Now weeks-left = round(timeLeft / 1 week), derived from actual depart date.
[2026-06-26] — Index countdown: count weeks on the Mon–Sun calendar grid (matches week-page date ranges, e.g. week-06 = 22–28 Jun) instead of raw elapsed/7. Added GRID0 = Mon 18 May (week-01 Monday); weekNo = floor((now-GRID0)/week)+1; weeks-lived = weekNo-1, weeks-left = 11-weekNo. Weeks now flip when Sunday ends.
[2026-06-26] — Index countdown: "days to go" stat now shows 2 decimals (left/DAY).toFixed(2) e.g. 6.54 instead of ceil whole days. Cosmetic/fun.
[2026-06-26] — Index countdown: weeks-lived and weeks-left now show 1 decimal (continuous off the GRID0 Monday anchor / time left); days-to-go dropped from 2 decimals to 1. Dropped the integer weekNo flip in favor of the live fractional value.
[2026-06-29] — Fix convert-upload.mjs: add sharp .rotate() so EXIF orientation baked into pixels; stops photos flipping vertical/sideways in webp output.
[2026-06-29] — party three-party.ts: load 3 new GLB props (Party_Glasses, Soda_Glass, Bass_Speakers) as side floaters — bob + drift + spin + mouse parallax, emissive lift so disco lights catch them.
[2026-06-29] — party photos section: new three-party-photos.ts on dedicated #photos-canvas (inside .party-photos, behind .party-inner). 8 cloned GLB props (glasses/soda/speakers) scattered down both sides, bob + spin + mouse parallax + scroll-velocity kick, party point lights. Photos stay on top.
[2026-06-30] — Week 07 retheme "Art and creativity" → "Goodbyes and the road home" (people leaving Erasmus). New muted palette (#6C8EBF / #E8B25A / #eef2f7) in weeks.ts + week-07.astro; rewrote section copy to departure arc. New three-week-07.ts: Car drives -Z on scroll along tiled Roa_block road, BlockMan friends dropped roadside fade out as car pulls away, House at road's end with warm PointLight that glows on arrival; chase cam + low dust. Passed modelPath=/models/Car.glb so three-week.ts skips its wireframe blob.
[2026-06-30] — Week 07 tweaks: rotate road tiles 90° (Math.PI/2 Y) so strip runs into -Z not sideways. Enabled glassContent on week-07 (transparent main so models show through). Wrapped sections in .depart-flow with scoped style: narrow 360px cards alternating left/right (nth-child odd/even) with 70vh gaps to keep centre clear for car/road/house; photos section stays full-width centred; mobile stacks full-width.
[2026-06-30] — Week 07 polish: road seam — added TILE_OVERLAP (0.99) factor on tileDepth in three-week-07.ts to close gaps between road tiles. Characters reworked from static/creepy to "departing souls": warm emissive glow (accent), idle sine bob while waiting, and as the car pulls away they fade + float upward (gone*4) + slow spin instead of standing frozen.
[2026-06-30] — Week 07 road: added rotation.x (pitch) + rotation.z (roll) correction knobs (default 0) beside the road rotation.y in three-week-07.ts, to flatten a baked tilt in the Roa_block model.
[2026-06-30] — Week 07 road: set rotation.z = Math.PI/2 to roll the Roa_block flat (was standing on its edge / sideways). Surface now faces up.
[2026-06-30] — Week 07 road: removed all rotation modifiers (x/y/z), load Roa_block model as-is.
[2026-06-30] — Week 07 cards: widened desktop floating bubbles 360px → 460px so titles stop overflowing. Mobile unchanged (100% via media query).

[2026-07-01] — Countdown: "weeks lived" used GRID0 (May 18) as base, gave 6.4; switched to START (May 20 arrival) so it reads correct 6.0. Removed now-unused GRID0.
[2026-07-01] — Week 11 theme (farewell): glass content mode + departing-plane scene (three-week-11.ts loads Airplane.glb, climbs up-and-away on scroll, bookend to week 01 landing). Added sections: By the numbers stat grid, reverse recap wall (links all prior weeks, greys unpublished), two-person goodbye letter. Stats/letters are placeholders to fill on last days.
[2026-07-01] — Week 11 plane redo: dropped loop-flight + shader contrail (too busy/janky). Simple takeoff — plane bigger (6/maxDim), sits low & level with tiny idle bob, scroll pitches nose up + climbs up-right + shrinks to dot. Single scroll-scrubbed value, no path/contrail.
[2026-07-02] — Week 11 plane bigger: baseScale 6→9/maxDim (larger at start), scroll shrink 0.85→0.72 so final resting size bigger than before.
[2026-07-02] — Week 11 finale overhaul: (1) plane scene drops 11 week-coloured glow-dot trail + sky light cycles through all 11 week colours as it climbs; (2) new 3D memory cloud (three-week-11-cloud.ts) loads real Supabase photos from every published week as billboarded planes, PINNED section flies camera front→back through whole cloud on scroll, mouse parallax; (3) hand-drawn SVG "The End" strokes itself in over final ~45% of flythrough then fills; (4) week-11-finale.ts: stat count-up on scroll-in, scroll reveals + stagger for sections/recap/letters, "OBRIGADO AMARANTE" letter-by-letter sign-off. Replaced placeholder emoji photo grid. All scoped to week-11, shared layout untouched.
[2026-07-02] — Week 11: memory cloud no longer hits Supabase (LOAD_PHOTOS=false flag in three-week-11-cloud.ts) — uses 24 week-coloured gradient placeholder cards instead, zero network requests; flip flag to true to restore real photos. Removed "Jose · Álvaro" names block from final sign-off card (kept OBRIGADO AMARANTE + year); cleaned orphaned CSS + finale JS.
[2026-07-02] — Week 11 memory cloud: added SINGLE_PHOTO mode — preloads one Supabase public object (week03-34.webp) ONCE and reuses the texture across all 24 cards. One network request total. Blank the const or load failure → colour placeholders.
[2026-07-02] — Week 11: suppressed empty space after "The End". Trimmed flythrough (Z_END -16→-11, pin +=220%→170%), The End now finishes drawing exactly at pin release. Removed cloud bottom margin + killed shared .week-content bottom padding via is:global :has(#memory-cloud) so footer sits flush under The End.
[2026-07-02] — Week 11 "Ten weeks, one story": rebuilt recap tiles as mini-heroes of each week — real bgColor, giant week number (display font, primary), Week badge + dates + subtitle, soft radial glow standing in for the 3D canvas, hover lift + glow/number scale, Revisit→/Soon CTA. Added readableFg() luminance helper so dark heroes (week 6) get light text via --fg. Pure CSS, no per-card 3D.
[2026-07-02] — Week 11 recap wall: reverted to num+subtitle tile layout, added a per-card <canvas> rendering that week's actual 3D hero (heroModel map: Airplane/Sun/Star/Bird/bridge/Car for wk1-7, wireframe sphere/cone/cylinder for wk8-10). New three-recap-cards.ts: one lightweight renderer per canvas, GLB fetched-once + cloned, IntersectionObserver so only visible cards animate, slow spin. ~12 WebGL contexts on the page total (under browser limit).
[2026-07-02] — Week 11 recap wall fixes: (1) root-caused missing models (Sun/Star/geos) to WebGL context limit — replaced 10 per-card renderers with ONE shared renderer using scissor rects (three.js many-items-one-context pattern), page now 3 GL contexts total; added Hemisphere light + ACES tone-mapping so all models read well. (2) Grid forced to 5 columns (5 and 5), 2 cols under 720px. (3) Nav-safe scissor clip so spinning models never paint over sticky nav. (4) Dropped recap-tile gsap stagger (overlay 3D cannot fade in lockstep). Confirmed hero map: wk1 Airplane, 2 Sun, 3/5 Star, 4 Bird, 6 bridge, 7 Car, 8-10 wireframe sphere/cone/cylinder.
[2026-07-02] — Week 11 recap wall: fixed "black box" / ghosting models. three-recap-cards.ts kept scissor test enabled during the once-per-frame renderer.clear(), so gl.clear only wiped the last card's rect and old model pixels smeared across the fixed z-index:12 overlay while scrolling. Now toggle setScissorTest(false) around clear() to wipe the whole (transparent) buffer each frame, then re-enable for per-card scissor rendering. Layout untouched.
[2026-07-02] — Week 11 recap wall: rewrote three-recap-cards.ts from scratch — dropped the fragile full-screen scissor overlay (was turning the whole screen black). Now ONE off-screen shared WebGLRenderer draws each week's hero (GLB or wireframe geo) and the frame is copied via 2D drawImage into that card's own in-flow <canvas>. Single WebGL context, no overlay/scissor/z-index tricks; off-screen cards skipped, backing store matched to CSS size × dpr. Layout untouched. Build passes.
[2026-07-02] — Week 11 recap wall: fixed blank models (Airplane/Bird/bridge/Car showing nothing while Sun/Star worked). Root cause in frame() — it centred (position -= worldCenter) BEFORE scaling, but those GLBs have geometry far from their local origin, so scaling about the root re-introduced a huge offset (child_world = center × (scale−1)) that flung the mesh off-screen. Reordered: scale first, THEN recompute bbox + re-centre. Confirmed via bbox logging (Airplane raw size ~1566×580×1675, Bird ~179×241×57 etc). Removed debug logging/gigantic target. Build passes.
[2026-07-02] — Week 11 recap wall: bumped on-card size of Airplane/bridge/Car (2.4 → 3.1) via new MODEL_TARGET override map in three-recap-cards.ts; others unchanged.
[2026-07-02] — Week 11 finale animations: (1) each recap tile now reveals with its own elegant entrance — cycle of 10 distinct GSAP moves (rise, glide L/R, book-open rotationY, bloom scale, hinge-up rotationX, drop, swing settle, skew, clip-wipe), each on its own scroll trigger for a varied cascade (possible now that tiles own their canvas in-flow, no overlay). (2) Goodbye letters (Jose/Álvaro) now typewrite char-by-char on scroll-in, second after the first, with a blinking accent caret (.type-caret::after, caretBlink keyframes in week-11.astro; caret removed on complete). Build passes.
[2026-07-02] — Week 11 finale animations, corrected target: removed all entrance animation from the 3D recap tiles (left the WebGL wall untouched — also skip the recap section in the content-section reveal). Moved the varied elegant entrances onto the real page cards (.stat-cell + .letter) via cardMoves cycle. Fixed early triggering — content-section 82%→68%, cards 72%, letter typewriter 78%→66% — so nothing pops in before it's properly in view. Build passes.
[2026-07-02] — Week 11 cards: fixed the "card visible → disappears → reappears on scroll" glitch. Cause: gsap.from() + ScrollTrigger re-applied its hidden start state on every ScrollTrigger refresh (the pinned hero + memory-cloud sections fire refreshes as you scroll), plus a first-paint flash before JS ran. Rebuilt the reveals: removed the generic content-section fade entirely; stat-cells + letters now start opacity:0 in CSS (no flash) and animate via gsap.fromTo + immediateRender:false + once (a refresh can't revert a played card). Each card keeps its distinct entrance from cardMoves. Typewriter unchanged. Build passes.
[2026-07-02] — Week 11 cards: swapped the scroll-in entrances for perpetual "living" ambient motion (pure CSS, no ScrollTrigger → nothing can flash/revert). Stat cells: slow float + breathing week-colour glow + diagonal sheen sweep (::after) + icon bob, all with per-nth-child delay offsets so the grid undulates organically. Goodbye letters: gentle drift + pulsing accent edge-glow (::before). Removed the fromTo entrance JS and the opacity:0 pre-hide. prefers-reduced-motion disables it all. Count-up + typewriter kept. Build passes.
[2026-07-02] — Week 11 containers + their text: added ambient CSS animations to the content-section cards — a shimmering accent bar (gradient, background-position slide) along each card's top edge (alternating delay on even sections), section-tags get a slow colour shimmer through the letters (gradient background-clip:text), and h2 headings get a soft white glint sweeping across (gradient text sheen). All scoped to week-11, no ScrollTrigger. prefers-reduced-motion disables them and restores solid tag/heading fills for readability. Build passes.[2026-07-03] — Week 11 "By the numbers": replaced the floating stat-card grid with a departure-board ledger. One full-width row per stat: oversized gradient count-up number (clamp up to 5rem, week primary→accent gradient text) left, mono uppercase label centre, big faded icon right, with a colour accent line perpetually sweeping left→right under each row (staggered per-row delays). Hover nudges the row + tints its background. Reused the existing .stat-value class so the finale count-up script keeps working; icon keeps its bob. Removed the old .stat-grid/.stat-cell card + sheen/float/glow CSS; updated the prefers-reduced-motion block to the new selectors.
[2026-07-03] — Week 11: replaced the "By the numbers" stats section entirely with a self-drawing JOURNEY MAP. New editable `journey` array (place/note/x/y%) in the frontmatter → precomputed SVG points + route path. Inline SVG on a dotted "map board": a glowing route-line (pathLength=1), a stop marker per place (pulsing ring + dot + mono name/note, alternating above/below the line), and a ✈ plane glyph. New script src/scripts/week-11-journey.ts (gsap + ScrollTrigger + MotionPathPlugin): on scroll-in (once) the route draws via strokeDashoffset while the plane flies it along the same path (MotionPath, autoRotate -45 to correct the glyph), and each stop pops in staggered; plane fades out on landing. prefers-reduced-motion shows the final drawn state, no motion. Removed the earlier ledger markup/CSS; the finale count-up now no-ops (no .stat-value left). Build passes.
[2026-07-03] — Week 11: dropped the journey map (we barely left Amarante — wrong premise) and shipped two goodbye-themed pieces instead. (1) SPLIT-FLAP DEPARTURE BOARD replaces the old stats slot: dark airport board, three editable lines (OBRIGADO AMARANTE / DEPARTURE 31 JUL / GATE … HOME) padded to 17 cols; each cell rolls through the character set and "clacks" (a quick vertical fold via WAAPI) onto its letter, columns rippling left→right. New src/scripts/week-11-splitflap.ts, IntersectionObserver once, reduced-motion shows final letters. (2) DISSOLVING GOODBYE as a new card lower down (after the two letters, before the memory cloud): a 2D-canvas particle field sampled from the word "ADEUS" that lifts, fans upward and fades when scrolled into view — the goodbye blows away. New src/scripts/week-11-dissolve.ts, once, reduced-motion leaves the word static. Removed week-11-journey.ts and all journey/ledger CSS. Build passes.
[2026-07-03] — Week 11: removed the dissolving "ADEUS" goodbye card — it interfered with the 3D model/memory-cloud sections. Deleted the card markup, its CSS, the script import, and src/scripts/week-11-dissolve.ts. Split-flap departure board kept. Build passes.
[2026-07-03] — Week 11: replaced the "By the numbers" stats section with a BOARDING PASS. Real ticket: airline header, AMT → CASA route with a taxiing ✈ on a dashed line, passenger/flight/date/gate/seat/boarding detail grid, and a tear-off stub (dashed seam, punched notches via CSS mask cut-outs, generated barcode, seat/gate mini-cells). Holographic sheen sweeps forever; on hover a pointer-tracked glare + 3D tilt (new src/scripts/week-11-boardingpass.ts, MAX 12°); flips up into place on scroll-in (IntersectionObserver once). Data + barcode in the frontmatter `pass` object. prefers-reduced-motion shows it flat, no tilt/sheen. (Note: page had been reverted to original before this, so the earlier split-flap markup is gone; src/scripts/week-11-splitflap.ts is now an unused orphan.) Build passes.
[2026-07-03] — Week 11 boarding pass: added a 1.5px week-primary-tinted border around the ticket for definition against the glass/3D background. Build passes.
[2026-07-03] — Week 11 boarding pass: fixed the borderless dents/corners and changed the route. Replaced the CSS mask cut-out notches (which erased the border at the dents) with real bordered discs (.pass-notch top/bottom) filled with --week-bg and given the same 1.5px border as the ticket; .pass now uses overflow:hidden to clip the outer half into an outlined bite, so corners and dents both keep a continuous outline. Route changed: OPO Oporto → BCN Barcelona (was AMT Amarante → CASA Home). Build passes.
[2026-07-03] — Week 11 boarding pass: updated ticket data — flight VY8481, boarding/departure 22:00, seat 15F, gate --. Build passes.
[2026-07-03] — Week 11 boarding pass: shrank the barcode so it fits the stub — 46→30 bars, gap 2px→1px, added max-width:100%. Build passes.
[2026-07-03] — Week 11 memory-cloud "The End": fixed the jump/teleport at the end of the flythrough. (1) camera Z now tracks the scrubbed flight.z directly instead of an extra per-frame lerp (*0.08) that overshot and drifted ~1s after you stopped scrolling. (2) Added anticipatePin:1 so pin engage/release doesn't snap. (3) "The End" now finishes drawing by ~85% of the pin (d = (p-0.45)/0.4) so it holds fully drawn before the pin releases, instead of completing exactly at release. Build passes.
[2026-07-03] — Week 11: made the page end on "The End". The week-nav footer (rendered by WeekLayout after <main>) was the thing sliding in under the memory-cloud flythrough when the pin released. Hid it on week-11 via <style is:global> .week-footer{display:none}. With no footer, the pinned scene's post-release resting frame equals its last pinned frame (canvas fills the 100vh section, "The End" centred), so the page just stops on it. Kept the sticky refactor idea off — html/body overflow-x:hidden would break position:sticky. Build passes.
[2026-07-03] — Week 11: added overflow:hidden to #memory-cloud to clip the .the-end SVG's overflow:visible stroke overshoot, which was pushing a stray scrollbar at the final section. Build passes.
[2026-07-03] — Week 11 finale: lock scroll once the flythrough completes. On the pinned memory-cloud ScrollTrigger, onLeave adds html.finale-locked (overflow:hidden on html+body) so the page freezes dead-still on "The End" with no drift and no stray scrollbar. Releases on scroll-up intent: wheel deltaY<0, ArrowUp/PageUp/Home, or touch swipe-down; onEnterBack also unlocks as a safety.
[2026-07-03] — Week 11 finale: reverted the scroll-lock (it set html/body overflow:hidden, which removed the scrollbar → viewport widened ~15px → centered content shifted "a bit down/right" on lock and back on scroll-up, and could trap the user). Removed the finale-locked class, wheel/key/touch listeners and CSS, and onLeave/onEnterBack. Scroll now stays free both ways. Also smoothed "The End": draw spread across p 0.4→0.95 (was 0.45→0.85) and opacity now ramps in (clamp01(d*6)) instead of a hard d>0 cut, so it fades instead of popping.
[2026-07-03] — Week 11 finale: fixed the viewport-dependent jump at "The End". Recap canvases + the memory cloud load async, so ScrollTrigger measured the pin start/end against a stale/shorter layout — the offset (hence the jump) scaled with viewport, so it vanished at some sizes. Added invalidateOnRefresh:true on the pinned trigger and a ScrollTrigger.refresh() after init plus one on window load, so pin math is recomputed against the real settled layout.
[2026-07-03] — Week 11 finale: shrank #memory-cloud from 100vh to 82vh so the final section sits under the viewport height and stops spawning the stray scrollbar at the end.
[2026-07-03] — Week 11 finale: fixed the last "The End" teleport + scrollbar flick at page bottom. Root cause found via headless-browser probe: the goodbye-letter typewriter (week-11-finale.ts) clears each .letter p then retypes it, so every line-wrap regrew the page ~28px AFTER the memory-cloud pin had measured its start/end; the late growth (browser scroll-anchored at the bottom) pushed maxScroll past the pin end, releasing the pin mid-hold and jumping the SVG ~68px. Fix: lock each paragraph's final height (min-height = getBoundingClientRect().height) before clearing, so typing never changes layout. Verified: document height now constant through the whole scroll, pin holds, SVG static.
[2026-07-06] — Week 11 memory cloud: now loads real Supabase photos from every week (LOAD_PHOTOS=true, SINGLE_PHOTO disabled). Discovers folders live by listing the Photos bucket root, so new folders/photos appear automatically on next visit. Picks PER_WEEK random photos per folder (Fisher-Yates), round-robin interleaves weeks, then shuffles — varied mix, different each load. Removed now-unused published-weeks-11 JSON injection.
[2026-07-06] — Party page: replaced the static photo grid + "show more" button with neon film strips — up to 3 full-bleed rows of photos auto-drifting in opposite directions (seamless loop via duplicated track, wrap at half-width). Scroll velocity adds a temporary speed boost + slight skew tilt; hovering a row pauses it and the photo lifts with a pink/purple glow; click still opens the existing lightbox. Rows are edge-faded with a mask. prefers-reduced-motion → static swipeable rows, no drift. Removed unused ScrollTrigger import and INITIAL batching from the party photo script.
[2026-07-06] — Party page fix: film strips rendered broken (gigantic unstyled photos, heavy lag). Root cause: the .film-* CSS sat in the scoped <style> block, but the strip nodes are created in JS so they never get Astro's data-astro-cid attribute → none of the CSS matched. Moved the film-strip CSS into the is:global block. Also capped decoded photos at 10 per row (eager full-res decodes were unbounded) with img.decoding=async, and dropped the per-frame skewX tilt (skew changes re-rasterise the track layer every frame; translateX-only stays composite-only).
[2026-07-06] — Week 11 perf: page no longer loads all cloud photos up front. three-week-11-cloud.ts now (1) defers ALL init — Supabase listing, photo fetches, WebGL context, ScrollTrigger pin — until the user is within 1.5 viewports of #memory-cloud (IntersectionObserver); (2) downscales every photo to max 512px via canvas before GPU upload (full-res ~4000px photos were ~48MB VRAM each — the main lag source), using img.decode() for async decode, no mipmaps; (3) loads photos through a 4-worker queue instead of ~40 parallel fetches; (4) skips rendering while the section is offscreen. three-recap-cards.ts: deferred scene build (7 GLB fetches) until the recap wall is within half a viewport.
[2026-07-07] — Week 07: rewrote the four narrative paragraphs to sound more human, less AI. Cut cliches ("grateful for the time we had", "won't be sad because it ended, happy because it happened"), tightened phrasing, kept the same section structure/order.
[2026-07-09] — Week 08 re-themed from "Portuguese cuisine" to "The world came to Amarante" (many nationalities visited). New passport/travel-document style: kraft-paper bg (#e9e0cd), ink-blue primary (#1B3A5C), stamp-red accent (#B23A2E). Content rebuilt in week-08.astro: MRZ data-page card, circular country "stamps" grid, multilingual hello grid, dashed passport photo frames — all scoped CSS, no shared files touched. Synced weeks.ts entry (subtitle/dates/colors). glassContent left off (opaque paper cards, not glass).
[2026-07-09] — Week 08: replaced the passport data-page opener with a boarding-pass stub (FROM Everywhere → TO Amarante, tear-off seat 08). Same ink/kraft/mono style kept; only the top card + its CSS swapped in week-08.astro.
[2026-07-09] — Week 08: dropped the boarding-pass opener (user: no document-like elements). Replaced with a split-flap ARRIVALS BOARD — dark ink panel, cream flap tiles with center seam, each origin flips in staggered (rotateX) with a blinking live dot + green "Landed" status. Folded the circular stamps grid into the board (same country roster) and removed it. Reduced-motion honored. Removed now-unused note field from stamps array.
[2026-07-09] — Week 08 arrivals board: countries → English + real roster (Armenia, Morocco, Russia, Mexico, Spain / ARM MAR RUS MEX ESP). Flips now trigger on scroll: tiles sit hidden (rotateX -90) until IntersectionObserver (three-week-08.ts, threshold 0.35) adds .in-view, which runs the staggered ab-flip. One-shot (disconnect after fire); reduced-motion shows tiles flat.
[2026-07-09] — Week 08 3D: loaded arrivals2.glb on #three-canvas via new three-week-08.ts (vanilla Three + GSAP; set modelPath prop so three-week.ts skips its wireframe). Nested groups isolate each animation: tilt(parallax ±0.05rad lerp) > entry(ScrollTrigger from scale .85/y-.8/opacity0 → 1, 1.4s power3.out) > float(±0.18 y, 6.5s yoyo) > breathe(±0.02rad y, 5.2s yoyo) > model. Suitcase_* wrapped → Z sway ±0.018; Pin_* → elastic Y bounce; Network_Node_01-06 → staggered scale pulse; Network_Line_01-09 → scale.y 0→1 stagger at end of entrance; Particle_* reparented → orbit (dark faster than light) + own-axis spin. Premium lighting: ambient + shadow-casting directional, PCFSoftShadowMap, Base_Platform receives. Low camera for imposing hero. resize + astro:before-swap cleanup (cancel rAF, kill ScrollTriggers, dispose). Build passes.
[2026-07-09] — Week 08 3D renderer/light polish: added renderer.setClearColor(0x000000, 0) for fully transparent canvas (cream HTML bg shows through, no gray edges; alpha:true was already set). Softened shadow via key.shadow.intensity=0.55 and added a HemisphereLight (white sky / navy #1B3A5C ground) so base shadows read as subtle transparent navy instead of harsh black. GSAP anims, model loading, layout untouched.
[2026-07-09] — Week 08 title/color rollout: subtitle → "The world in one place" everywhere (weeks.ts, week-08.astro prop, three-week-08.ts header). gallery.ts week 8 updated from "Portuguese cuisine"/#FF9800 → "The world in one place"/#1B3A5C to match the new navy/red/kraft palette. Build passes.

[2026-07-09] — Week 08 two new sections. (1) Distance count-up: added km per country to stamps (+ totalKm/maxKm); .distance section shows 18,180 km total that tweens 0→target on scroll (GSAP + IntersectionObserver, toLocaleString), plus per-country bars filling to km/maxKm via .in-view CSS transition. (2) Connection globe: new src/scripts/globe-week-08.ts renders a bounded #globe-canvas (not the fullscreen layout canvas) — faint navy fill + wireframe sphere, navy pins per arrival country, accent Amarante pin with pulsing halo, QuadraticBezier arcs country→Amarante that draw in (setDrawRange tween, staggered) when scrolled into view. Auto-rotate + pointer drag to spin; resize + astro:before-swap cleanup. Passport palette (navy/accent/cream). Build passes.
[2026-07-09] — Week 08: replaced the distance km count-up with live timezone clocks. New `clocks` array (Amarante/Lisbon home + Spain/Morocco/Russia/Armenia/Mexico, IANA tz ids); removed km/totalKm/maxKm from stamps. .clocks section renders a grid of analog SVG clock-faces (hour/min/sec hands rotated via CSS transform, transform-box:view-box origin 50px) + digital HH:MM + weekday. Inline script ticks every 1s: reads each zone via Intl.DateTimeFormat.formatToParts (DST-safe), rotates hands, sets time/day, toggles .is-night (07:00–21:59 = day ☀ else ☾) so night cards dim to dusk navy — some awake, some asleep at the same second. Home card gets accent frame. clearInterval on astro:before-swap. Globe section untouched. Build passes.
[2026-07-09] — Week 08: removed "Eight ways to say hi" Languages section + orphaned hellos[] data and .hello-grid/.hello/.hello-lang/.hello-word CSS.
[2026-07-09] — Week 08 globe: added continent silhouettes as a dot-matrix. Downloaded Natural Earth 110m land to public/data/land-110m.geojson; globe-week-08.ts fetches it, flattens Polygon/MultiPolygon outer rings, stipples a navy THREE.Points dot over every land cell of a lat/lon grid (STEP 2.4deg, lon spacing widened by 1/cos(lat) near poles) via ray-cast point-in-polygon. Replaced the full wireframe sphere with a paper-colored (#f3ecdb) opaque occluder (hides far-hemisphere dots, blends into section bg) + one faint 24x12 meridian shell (opacity 0.12). Dots at R*1.004, pins/arcs render on top. Fetch failure caught (decorative). Build passes.
[2026-07-09] — Week 07: reworded the four narrative paragraphs again for a more natural human voice — added small imperfections (fragments, "honestly", contractions), varied rhythm, dropped remaining stiff phrasing.
[2026-07-09] — Week 07: swapped em dashes in prose for plain commas/periods.
[2026-07-09] — Week 08 reorder for narrative flow: intro prose moved to top (context first; section-tag "Arrivals"→"The week" to kill the duplicate tag), then arrivals board → connection globe → timezone clocks → photos. Escalates info→feeling. Markup-only move; no logic/CSS change. Build passes.
[2026-07-09] — Week 08 GSAP scroll text reveals. Page script now imports gsap + ScrollTrigger: every .passport h2 is split into .rv-word inline-block spans (spaces preserved as text nodes) that stamp in per-word (yPercent 120→0, opacity, back.out(1.6), stagger 0.05) on scroll; .section-tag + p rise (y22→0, opacity, power3.out) once at top 90
[2026-07-09] — Week 08 GSAP scroll text reveals. Page script imports gsap + ScrollTrigger: every .passport h2 split into .rv-word inline-block spans (spaces kept as text nodes) that stamp in per-word (yPercent 120->0, opacity, back.out(1.6), stagger 0.05) on scroll; .section-tag + p rise (y22->0, opacity, power3.out) once at top 90pct. prefers-reduced-motion guard skips all. Copy unchanged. Build passes.

[2026-07-13] — Week-08: added Poland, Georgia, Ukraine, Hungary, Azerbaijan to arrivals board, globe pins/arcs, and timezone clocks (Armenia/Mexico already present, kept as-is).

[2026-07-13] — Week-08: removed Russia from arrivals board, globe, clocks, and intro copy.

[2026-07-13] — party.astro: removed 30-photo cap in buildStrips (was MAX_PER_ROW*ROWS slice); all party bucket photos now render.

[2026-07-13] — Week-08: intro section redesigned as passport bio-page — corner ink stamp (circular text + week seal), drop-cap opening paragraph, inline nationality chips (Spanish/Morocco/Armenia).

[2026-07-13] — Week-08: arrivals board stagger delay now generated per-row (--ab-delay inline var) instead of hardcoded nth-child(2-6); all 9 countries flip-animate, not just first 5.

[2026-07-13] — Week-08: removed static drop-cap (user disliked), replaced loc-chip styling with scroll-triggered stamp-thud animation (GSAP back.out, random rotate) for actual motion.
